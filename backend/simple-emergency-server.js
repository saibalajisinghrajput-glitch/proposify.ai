const express = require('express');
const cors = require('cors');

const app = express();
const PORT = process.env.PORT || 10000;

// Middleware
app.use(cors({
  origin: ['https://saibalajisinghrajput-glitch.github.io', 'http://localhost:3000'],
  credentials: true
}));
app.use(express.json());

// Health check endpoint
app.get('/api/health', (req, res) => {
  res.json({ 
    status: 'ok', 
    message: 'ProposifyAI Backend Running',
    timestamp: new Date().toISOString(),
    environment: process.env.NODE_ENV || 'development'
  });
});

// Test signup endpoint (no database)
app.post('/api/auth/signup', (req, res) => {
  const { email, password } = req.body;
  
  if (!email || !password) {
    return res.status(400).json({ 
      success: false, 
      message: 'Email and password are required' 
    });
  }

  // Mock successful signup
  res.json({ 
    success: true, 
    message: 'Test signup successful',
    user: { 
      email, 
      id: 'test-user-id-' + Date.now() 
    },
    token: 'test-jwt-token-' + Math.random().toString(36).substr(2, 9)
  });
});

// Test login endpoint
app.post('/api/auth/login', (req, res) => {
  const { email, password } = req.body;
  
  if (!email || !password) {
    return res.status(400).json({ 
      success: false, 
      message: 'Email and password are required' 
    });
  }

  // Mock successful login
  res.json({ 
    success: true, 
    message: 'Test login successful',
    user: { 
      email, 
      id: 'test-user-id-login' 
    },
    token: 'login-jwt-token-' + Math.random().toString(36).substr(2, 9)
  });
});

// Mock projects endpoint
app.get('/api/projects', (req, res) => {
  res.json({
    success: true,
    projects: [
      {
        id: 'test-project-1',
        name: 'Sample Project',
        description: 'This is a test project',
        createdAt: new Date().toISOString()
      }
    ]
  });
});

// Mock proposal generation endpoint
app.post('/api/proposals/generate', (req, res) => {
  const { projectName, description } = req.body;
  
  res.json({
    success: true,
    proposal: {
      id: 'test-proposal-' + Date.now(),
      projectName: projectName || 'Untitled Project',
      description: description || 'Sample description',
      content: 'This is a sample proposal generated by ProposifyAI.',
      createdAt: new Date().toISOString()
    }
  });
});

// Root endpoint
app.get('/', (req, res) => {
  res.json({
    message: 'ProposifyAI Backend API',
    status: 'running',
    endpoints: [
      'GET /api/health',
      'POST /api/auth/signup',
      'POST /api/auth/login',
      'GET /api/projects',
      'POST /api/proposals/generate'
    ]
  });
});

// Error handler
app.use((err, req, res, next) => {
  console.error(err.stack);
  res.status(500).json({ 
    success: false, 
    message: 'Something went wrong!' 
  });
});

// Start server
app.listen(PORT, () => {
  console.log(`🚀 ProposifyAI Backend running on port ${PORT}`);
  console.log(`🌍 Environment: ${process.env.NODE_ENV || 'development'}`);
  console.log(`🔍 Health check: http://localhost:${PORT}/api/health`);
  console.log(`📋 Available endpoints:`);
  console.log(`   GET  /api/health`);
  console.log(`   POST /api/auth/signup`);
  console.log(`   POST /api/auth/login`);
  console.log(`   GET  /api/projects`);
  console.log(`   POST /api/proposals/generate`);
});

// Export for testing
module.exports = app;

